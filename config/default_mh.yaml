project_name: multihead
device: cuda:0
random_state: 2022

# =========================== Dataset ===================
folds: [0]
imbalance_sampler: true
max_negative_examples: null
imb_sampler_num_samples: null

# =========================== Model =====================
model_type: "timm"
model_name: "tf_efficientnetv2_s_in21ft1k"
in_chans: 1
n_hidden: null

input_size: [150, 300]
test_input_size: [192, 384]
model_ckpt: null

drop_rate_back: 0.45
drop_rate_mh: 0.45

# ===================== Optimization =====================

lr: 3.0e-3
batch_size: 24
test_batch_size: 32
num_workers: 8
n_epochs: 40
weight_decay: 1.0e-4
label_smoothing: 0.0    # 0.0
optimizer: adamw
lr_scheduler: cosineannealing
loss_weights: [1, 0.5]

multi_cols: ["cancer", "invasive"]

# ===================== Augmentations ===================
severity: 5    # 3
mean: 0.5
std: 0.5
